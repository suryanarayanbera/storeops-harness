package com.cognizant.storeops.activities.domain;

/** Urgency of an operational activity. HIGH and CRITICAL are the SLA-tracked bands. */
public enum TaskPriority {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
