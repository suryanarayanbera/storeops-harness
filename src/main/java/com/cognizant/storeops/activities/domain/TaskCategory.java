package com.cognizant.storeops.activities.domain;

/** Kind of retail store work an activity represents. */
public enum TaskCategory {
    RESTOCKING,
    PLANOGRAM,
    AUDIT,
    COMPLIANCE,
    GENERAL
}
